# Handover: poe-acp-relay

## Current state (as of latest push)

- New standalone repo exists and is active:
  - **https://github.com/kfet/poe-acp-relay**
  - Current local path: `/Users/kfet/dev/ai/poe-acp-relay`
- Branch: `main`
- Latest commit includes all prior setup for standalone build + release + docs + handoff.
- Main module changed from subdir extraction to standalone:
  - `module github.com/kfet/poe-acp-relay`
  - Binary: `poe-acp-relay`
  - Main entry: `./cmd/poe-acp-relay`

## What’s already in place

### Code + docs moved into this repo
- `cmd/poe-acp-relay/`
- `internal/acpclient/`, `internal/httpsrv/`, `internal/poeproto/`, `internal/policy/`, `internal/router/`
- `docs/poe-acp-relay-design.md`
- `docs/poe-protocol-reference.md`
- `test/smoke.sh`
- `CHANGELOG.md`
- `AGENTS.md`

### Build / release infra added
- `.github/workflows/ci.yml`
  - runs `make all` and checks git clean.
- `.github/workflows/release.yml`
  - on `v*` tags: `make all` → `make notices` → GoReleaser.
- `.goreleaser.yaml`
  - builds 5 targets (darwin/arm64, darwin/amd64, linux/armv6, linux/arm64, linux/amd64)
  - raw binary archives (named `poe-acp-relay-<os>-<arch>`)
  - publishes release to `kfet/poe-acp-relay`
  - updates shared tap: `kfet/homebrew-fir` with formula `poe-acp-relay`
- `homebrew/poe-acp-relay.rb.template`
  - reference formula for the tap update; authoritative file is generated by GoReleaser.
- `Makefile`
  - target set adapted from fir:
    - `all`, `fmt`, `build`, `build-all`, cross-compile targets, `test`, `test-race`, `vet`, `notices`, `check-licenses`, `deploy`, `publish`
  - no Python tasks, no `generate-models`, no PGO profile targets.

### AGENTS and release skill
- `AGENTS.md` added (repo-specific guidance, no Python-specific constraints, no irrelevant fir sections).
- `.fir/skills/release/SKILL.md` added and tailored for this repo:
  - `make all`, changelog/date bump, `make install`, publish via `make publish` and release workflow monitoring.

### Docs cleanup already done
- Replaced references of old path/module:
  - `github.com/kfet/fir/external/poeacp` → `github.com/kfet/poe-acp-relay`
  - removed outdated `external/poeacp/` layout references in key docs.
- Copied one related doc from fir:
  - `acp-spec/rfd-auth-methods.md`

## Repo hygiene / known points

- `bin/` is ignored in `.gitignore`.
- `make all` passes in repo.
- `go test ./...` passes.
- `gh repo create` already run and initial history pushed to origin.

## Important commands

- Build/test:
  - `make all`
  - `make test`
  - `go test ./...`
- Run smoke test (manual):
  - `./test/smoke.sh`
- Release cycle:
  - `git tag v0.1.0` (or next version)
  - `git push origin v0.1.0`
  - GoReleaser handles GitHub release and tap update.

## Required secrets/integrations

- Add repo secret:
  - `HOMEBREW_TAP_TOKEN` in `kfet/poe-acp-relay`
- Token must be fine-grained and writable to `kfet/homebrew-fir` (Contents: read/write).

## Known limitations / to-dos for next agent

1. Add/update any missing docs around deployment (optional):
   - maybe separate `docs/deployment.md` for Funnel/systemd/tmux + `--poe-path` gotcha.
2. Consider whether more of fir `docs/` should be copied intentionally (currently only copied needed ACP-specific doc).
3. Verify first tagged release end-to-end with:
   - `brew install kfet/fir/poe-acp-relay`
4. If needed, add `README` smoke/quickstart steps for non-fir ACP agents (Claude Code, etc.).

## Contact/assumption notes

- Homebrew formula is published into existing `kfet/homebrew-fir` tap, same place as fir.
- This repo is intentionally minimal and focused only on Poe->ACP relay behavior.
- No further fir submodule/dependency links are expected.
